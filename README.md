# FontTuner 字体批量调整工具

## 使用方法

- 将需要调整的字体文件或文件夹拖放到程序上，会自动读取字体信息并保存到metadata.xlsx文件中
- 手动填写metadata.xlsx文件的关键列，如：usWeightClass、fsSelection及(16,x,x,x)和(17,x,x,x)等
- 将修改完成的metadata.xlsx文件拖放到程序上，会自动根据metadata.xlsx文件的信息调整字体文件的元数据
